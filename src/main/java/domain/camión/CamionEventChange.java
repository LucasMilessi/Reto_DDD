package domain.camión;

import co.com.sofka.domain.generic.EventChange;
import domain.camión.aggregate.Camion;

public class CamionEventChange extends EventChange {
    public CamionEventChange(Camion camion) {
    }
}
